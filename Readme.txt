To run app.py use "python app.py" in terminal
css is same for both index.html and login.html
'TEMPLATES' folder contain all the html files
'STATIC' folder contain JavaScript and Css and Images folder used in html
'Uploads' folder conatin the images which were uploaded to test the Working of the Deployed Model
'Models' folder conatin the .tflite model which is being Depoloyed.
